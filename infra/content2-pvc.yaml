apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: content2-pvc
  namespace: media
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany
  volumeMode: Filesystem
  resources:
    requests:
      storage: 1000Gi