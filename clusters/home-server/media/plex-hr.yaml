---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: plex
  namespace: media
spec:
  chart:
    spec:
      chart: plex
      reconcileStrategy: ChartVersion
      sourceRef:
        kind: HelmRepository
        name: k8s-at-home
      version: 6.2.2
  interval: 1m0s
  values:
    service:
      main:
        type: LoadBalancer
        loadBalancerIP: 192.168.1.210
        externalTrafficPolicy: Local
    env:
      allowedNetworks: 192.168.0.1/8
    persistence:
      content:
        enabled: true
        type: pvc
        existingClaim: content-pvc
      config:
        enabled: true
        type: pvc
        accessMode: ReadWriteOnce
        size: 1Gi